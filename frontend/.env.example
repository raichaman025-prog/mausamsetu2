# The frontend talks to the backend through the /api proxy defined in
# vite.config.ts during development, so no env vars are required to run
# the prototype. This file is a placeholder for a production build where
# you'd point directly at a deployed API origin.
VITE_API_BASE_URL=http://localhost:8000
